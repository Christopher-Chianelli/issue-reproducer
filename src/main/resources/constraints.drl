package org.optaplanner.issue.reproducer;
    dialect "java"

import org.optaplanner.issue.reproducer.Person;

rule "Print security alert when James Bond is detected"
    when
        Person(fullName == "James Bond")
    then
        System.out.println("James Bond detected.");
end